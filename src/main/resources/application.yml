
server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://db.upkqppgfhyfvzrebkggw.supabase.co:5432/postgres?sslmode=require
    username: postgres
    password: T3csup123456@@
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      idle-timeout: 30000
      connection-timeout: 30000
      validation-timeout: 5000

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false






